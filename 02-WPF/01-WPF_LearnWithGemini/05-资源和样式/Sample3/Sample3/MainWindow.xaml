<Window x:Class="Sample3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sample3"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="300" Background="{DynamicResource LightModeBackground}">
    <Window.Resources>
        <ResourceDictionary>
            <Style x:Key="BaseButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Blue"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Margin" Value="5,0,5,0"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="LightBlue"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="RoundButtonStyle" TargetType="Button">
                <Setter Property="Height" Value="25"/>
                <Setter Property="Width" Value="25"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid>
                                <Ellipse Width="{TemplateBinding Width}" Height="{TemplateBinding Height}"
Fill="{TemplateBinding Background}" Stroke="{TemplateBinding BorderBrush}"
StrokeThickness="{TemplateBinding BorderThickness}"/>
                                <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="LightBlue"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            <local:CompletedToOpacityConverter x:Key="CompletedToOpacity"/>
            <local:PriorityToColorConverter x:Key="PriorityToColor"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="14*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <UniformGrid Columns="3" Rows="1" HorizontalAlignment="Center">
                <Button Content="1" MinWidth="75" Style="{StaticResource BaseButtonStyle}"/>
                <Button Content="1" MinWidth="75" Style="{StaticResource BaseButtonStyle}"/>
                <Button Content="H"  Style="{StaticResource RoundButtonStyle}" Click="ChangeColor_CLick"/>
            </UniformGrid>
        </StackPanel>
        <Grid Grid.Row="1">
            <Button Content="添加任务" Command="{Binding AddTaskCommand}"/>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Vertical">
            <ListBox ItemsSource="{Binding TodoList}" Height="450">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="5"
                    Opacity="{Binding IsCompleted, Converter={StaticResource CompletedToOpacity}}">

                            <CheckBox IsChecked="{Binding IsCompleted}" VerticalAlignment="Center" Margin="0,0,10,0"/>

                            <TextBlock Text="{Binding Title}" 
                       Width="100" 
                       VerticalAlignment="Center"
                       Foreground="{Binding Priority, Converter={StaticResource PriorityToColor}}"
                       FontWeight="Bold"/>

                            <Button Content="X" 
                    Style="{StaticResource RoundButtonStyle}" 
                    Background="Red" 
                    Foreground="White"
                    CommandParameter="{Binding}" 
                    Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</Window>
